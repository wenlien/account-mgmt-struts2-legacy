# 複製成 .env 後填入實際值：cp .env.example .env
# .env 已被 .gitignore 忽略，不會進版控（S2：機敏值不硬編、不入庫）。
# 這些變數由 run.sh 載入後傳給 app：

# 主機設定（run.sh 開瀏覽器 / Tomcat 監聽用）
APP_HOST=localhost
APP_PORT=8080

# MySQL 連線密碼（spring-config.xml 以 ${DB_PASSWORD:} 取用）
DB_PASSWORD=change-me

# 登入帳密（SecurityConfig in-memory 認證；ADMIN_USERNAME 省略時預設 admin）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
