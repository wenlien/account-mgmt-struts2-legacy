# DB 連線設定（非機敏）。
# 密碼不在此檔：走環境變數 DB_PASSWORD（S2 不硬編密鑰），
# 由 Phase 2 的 Docker Compose / .env 注入；spring-config.xml 以 ${DB_PASSWORD:} 取用。
db.url=jdbc:mysql://localhost:3306/accountdb?useSSL=false&serverTimezone=UTC
db.username=accountuser
db.driver=com.mysql.jdbc.Driver
